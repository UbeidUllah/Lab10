function power(x,y) {
    return Math.pow(x,y);
}

function ceiling(x){
    return Math.ceil(x);
}

function round(x) {
    return Math.round(x);
}

module.exports.power = power;
module.exports.ceiling = ceiling;
module.exports.round = round;